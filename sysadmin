<!-- vim: syntax=Markdown -->
# System Administration

## Network

- List all current open ports with relative processe using one of these
  commands:
    ```bash
    sudo lsof -i
    sudo netstat -lptu
    sudo netstat -tulpn
    ```
